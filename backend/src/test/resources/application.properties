# The real connection details come from the Testcontainers @ServiceConnection bean.
# These placeholder values only exist so the main configuration resolves without a
# developer .env file present, which keeps the tests runnable on CI.
DB_URL=jdbc:postgresql://testcontainers/overridden
DB_USERNAME=overridden
DB_PASSWORD=overridden
